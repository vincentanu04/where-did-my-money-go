// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: expenses.sql

package sqlc

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const createExpense = `-- name: CreateExpense :one
INSERT INTO expenses (
  user_id,
  category,
  amount,
  expense_date
)
VALUES (
  $1, $2, $3, $4
)
RETURNING
  id,
  user_id,
  category,
  amount,
  expense_date,
  created_at
`

type CreateExpenseParams struct {
	UserID      uuid.UUID `json:"user_id"`
	Category    string    `json:"category"`
	Amount      int32     `json:"amount"`
	ExpenseDate time.Time `json:"expense_date"`
}

func (q *Queries) CreateExpense(ctx context.Context, arg CreateExpenseParams) (Expense, error) {
	row := q.db.QueryRow(ctx, createExpense,
		arg.UserID,
		arg.Category,
		arg.Amount,
		arg.ExpenseDate,
	)
	var i Expense
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Category,
		&i.Amount,
		&i.ExpenseDate,
		&i.CreatedAt,
	)
	return i, err
}

const listExpensesByUserAndRange = `-- name: ListExpensesByUserAndRange :many
SELECT id, user_id, category, amount, expense_date, created_at
FROM expenses
WHERE user_id = $1
  AND expense_date >= $2
  AND expense_date <  $3
ORDER BY
  category ASC,
  created_at DESC
`

type ListExpensesByUserAndRangeParams struct {
	UserID        uuid.UUID `json:"user_id"`
	ExpenseDate   time.Time `json:"expense_date"`
	ExpenseDate_2 time.Time `json:"expense_date_2"`
}

func (q *Queries) ListExpensesByUserAndRange(ctx context.Context, arg ListExpensesByUserAndRangeParams) ([]Expense, error) {
	rows, err := q.db.Query(ctx, listExpensesByUserAndRange, arg.UserID, arg.ExpenseDate, arg.ExpenseDate_2)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Expense
	for rows.Next() {
		var i Expense
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Category,
			&i.Amount,
			&i.ExpenseDate,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
